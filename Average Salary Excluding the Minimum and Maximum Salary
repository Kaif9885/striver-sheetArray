public:
    double average(vector<int>& salary) {

     double min = salary[0],max =salary[0];
     for(double i = 0; i<salary.size();i++)
     {
        if (salary[i] > max)
        max = salary[i];
        if(salary[i]<min)
        min = salary[i];
     }
     return (max+min)/2.0;
    }
};
